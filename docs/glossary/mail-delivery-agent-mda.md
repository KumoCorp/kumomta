# Mail Delivery Agent (MDA)

A Mail Delivery Agent (MDA) is the component that takes a message from the final receiving MTA and writes it into the recipient's mailbox. MDAs matter on the inbound/mailbox side of email; outbound sending infrastructure generally ends at the receiving MTA.
