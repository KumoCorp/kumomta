# DMARC alignment

Alignment is DMARC's requirement that the domain that passed SPF or DKIM must match the visible From domain (exactly, or sharing the registrable domain in relaxed mode). Alignment is what connects infrastructure-level authentication to the identity the recipient actually sees.
