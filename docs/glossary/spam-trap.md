# Spam trap

A spam trap is an email address that exists to catch senders with bad list practices, either a never-valid address (pristine trap) or a long-dead recycled address. Trap hits are strong negative reputation signals because no legitimate consented list should contain them; they indicate purchased lists or absent hygiene.
