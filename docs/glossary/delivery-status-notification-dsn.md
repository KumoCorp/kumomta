# Delivery Status Notification (DSN)

A Delivery Status Notification (DSN, RFC 3464) is the machine-readable report a mail server sends to a message's return-path address to report a failed or delayed delivery: the classic "bounce message." DSNs are how failures that happen after a message was accepted get reported back to the sender.
