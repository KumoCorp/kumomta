# Virtual MTA (VMTA)

A virtual MTA is a logical sending identity within one MTA instance: a binding of source IP, hostname, and policy that lets a single server behave as many distinct senders. The term was popularized by PowerMTA and remains common vocabulary for per-IP and per-identity sending configuration.
