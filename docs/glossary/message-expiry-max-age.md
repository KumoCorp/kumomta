# Message expiry (max age)

Message expiry is the point at which an undeliverable message stops retrying and is returned as a bounce. Transactional mail typically expires in hours (a stale password reset is worthless); promotional mail may retry for a day or more.
