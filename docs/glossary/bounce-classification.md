# Bounce classification

Bounce classification is the parsing of rejection responses into structured categories (bad address, spam block, mailbox full, policy rejection, and so on) so that suppression, alerting, and reputation decisions can be automated. Because every mailbox provider words its rejections differently, real-world classification depends on maintained rule sets rather than status codes alone.
