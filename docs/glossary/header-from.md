# Header From

The Header From (RFC 5322.From) is the sender address displayed to the recipient in their mail client. It can differ from the envelope sender; DMARC exists to bind the visible From domain to authentication performed on the envelope and signatures.
