# Authentication-Results header

Authentication-Results (RFC 8601) is the header a receiving server adds to record the outcome of its SPF, DKIM, DMARC, and ARC checks on a message. It is where you look, in a delivered message, to see how the receiver actually judged your authentication rather than how you intended it.
