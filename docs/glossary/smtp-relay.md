# SMTP relay

An SMTP relay is an MTA that accepts mail from one system and forwards it toward its destination rather than delivering it locally. "Relay" describes a role, not a product: an application server might relay through a central corporate MTA, which relays to the internet. An open relay, one that forwards mail for anyone, is a serious misconfiguration exploited by spammers.
