# PTR record (reverse DNS)

A PTR record maps an IP address back to a hostname. Mailbox providers expect every sending IP to have valid reverse DNS that resolves back to the same IP (forward-confirmed reverse DNS, FCrDNS); IPs without it are widely rejected or heavily penalized.
