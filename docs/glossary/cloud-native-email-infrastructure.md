# Cloud-native email infrastructure

Cloud-native email infrastructure is sending infrastructure built to run in containers and orchestrators (Docker, Kubernetes) with API-driven configuration, externalized state, and metrics-first observability, as opposed to hand-configured pet servers. The friction points are cloud-specific: port-25 egress policy, stable egress IPs, and PTR control.
