# Bounce

A bounce is a failed delivery. A hard bounce is a permanent failure (5xx: the address doesn't exist, the message is rejected); a soft bounce is a temporary failure that persists until the message exhausts its retries and expires. Continuing to send to addresses that hard-bounce is one of the fastest ways to destroy sender reputation.
