# SMTP envelope

The SMTP envelope is the delivery information exchanged during the SMTP transaction, the MAIL FROM (sender) and RCPT TO (recipients), as distinct from the message's visible headers. Routing and bounces are governed by the envelope, not by the From header a recipient sees; the two can legitimately differ.
