# Feedback loop (FBL)

A feedback loop is a mailbox provider's mechanism for reporting recipient spam complaints back to the sender, traditionally as ARF-format reports tied to registered IPs or domains (Microsoft's JMRP, Yahoo's CFL) or, at Gmail, as aggregate data via Postmaster Tools. Processing FBLs, and suppressing complainers immediately, is mandatory hygiene for any volume sender.
