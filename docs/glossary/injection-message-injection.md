# Injection (message injection)

Injection is the act of submitting a message into sending infrastructure for delivery, whether over SMTP or an HTTP API. High-volume MTAs are measured in part by injection throughput: how fast upstream applications can hand them mail.
