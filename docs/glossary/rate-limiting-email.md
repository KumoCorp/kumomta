# Rate limiting (email)

Rate limiting is capping the flow of mail (messages per second, per connection, per destination, per IP) to stay within what receiving providers will accept. In sending infrastructure, rate limits are set both globally and per destination; mailbox providers enforce their own undocumented, reputation-dependent limits from the other side.
