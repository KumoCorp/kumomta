# Suppression list

A suppression list is the set of addresses a sender must not mail: hard bounces, unsubscribes, complainers, and known traps. Suppression is enforced at send time, ideally in the sending infrastructure itself, so no upstream mistake can re-mail a suppressed address.
