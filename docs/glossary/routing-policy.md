# Routing policy

Routing policy is the logic that decides how each message is sent: which IP pool and signing identity to use, which queue it joins, whether to relay through another host, how to handle a given destination. Modern MTAs expose routing as code or configuration, and the programmability of routing policy is a primary differentiator among MTAs.
