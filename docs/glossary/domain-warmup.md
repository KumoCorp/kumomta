# Domain warmup

Domain warmup is the same gradual-ramp practice applied to a new sending domain or subdomain. Because domain reputation now dominates filtering decisions, new domains need warming even on established IPs.
