# kumo.string.starts_with

```lua
kumo.string.starts_with(STRING, PATTERN)
```

{{since('dev')}}

Returns a true if STRING_1 starts with PATTERN, otherwise false.

```lua
kumo.string.starts_with('hello world', 'hello')
```