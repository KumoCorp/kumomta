# kumo.string.ends_with

```lua
kumo.string.ends_with(STRING, PATTERN)
```

{{since('dev')}}

Returns true if STRING ends with PATTERN, otherwise false.

```lua
kumo.string.ends_with('hello world', 'world')
```
