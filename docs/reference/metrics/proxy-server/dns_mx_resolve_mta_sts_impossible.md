# dns_mx_resolve_mta_sts_impossible

```
Type: Counter
```
Total number of MailExchanger::resolve calls that failed because the  domain published an MTA-STS enforce policy that permits none of its own  MX hosts. Such domains are undeliverable until they fix their policy.

