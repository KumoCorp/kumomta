[package]
name = "throttle"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0"
mod-redis = {path="../mod-redis"}
once_cell = "1.17"
redis-cell = { git = "https://github.com/brandur/redis-cell.git" }
serde = {version="1.0", features=["derive"]}
time = "0.1" # for embedding redis-cell
thiserror = "1.0"
